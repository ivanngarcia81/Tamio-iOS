// `LogoMembrete` se mudó a `Support/PiezasPDF.swift`, con el resto del papel
// que comparten los documentos, para que también lo compile el Mac. Este
// archivo queda vacío porque sigue dado de alta en el `.pbxproj`, que solo
// edita el lead; se puede quitar de ahí cuando convenga.
